services:
  postgres:
    container_name: gestao_vagas_postgres
    image: postgres
    env_file:
      - .env
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB}